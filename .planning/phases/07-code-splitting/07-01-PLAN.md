---
phase: 07-code-splitting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/tech-stack.ts
  - components/marquee-text.tsx
  - components/animated-counter.tsx
  - components/github-activity.tsx
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "MarqueeText component is importable from components/marquee-text.tsx"
    - "AnimatedCounter component is importable from components/animated-counter.tsx"
    - "GitHubActivity component is importable from components/github-activity.tsx"
    - "techStack data is importable from data/tech-stack.ts"
    - "page.tsx is under 400 lines"
    - "Application renders identically to before"
  artifacts:
    - path: "data/tech-stack.ts"
      provides: "Tech stack data array with TechItem type"
      exports: ["techStack", "TechItem"]
    - path: "components/marquee-text.tsx"
      provides: "Animated marquee text component"
      exports: ["MarqueeText"]
    - path: "components/animated-counter.tsx"
      provides: "Animated number counter component"
      exports: ["AnimatedCounter"]
    - path: "components/github-activity.tsx"
      provides: "GitHub activity feed component with types"
      exports: ["GitHubActivity"]
    - path: "app/page.tsx"
      provides: "Main page with imports from extracted files"
      contains: "import { techStack }"
  key_links:
    - from: "app/page.tsx"
      to: "data/tech-stack.ts"
      via: "named import"
      pattern: "import.*techStack.*from.*data/tech-stack"
    - from: "app/page.tsx"
      to: "components/marquee-text.tsx"
      via: "named import"
      pattern: "import.*MarqueeText.*from.*components/marquee-text"
    - from: "app/page.tsx"
      to: "components/animated-counter.tsx"
      via: "named import"
      pattern: "import.*AnimatedCounter.*from.*components/animated-counter"
    - from: "app/page.tsx"
      to: "components/github-activity.tsx"
      via: "named import"
      pattern: "import.*GitHubActivity.*from.*components/github-activity"
---

<objective>
Extract inline components and data from page.tsx into dedicated files for maintainability.

Purpose: page.tsx is 826 lines with 4 inline components and a data array. Extracting these improves readability, enables reuse, and follows React best practices.

Output: 4 new files (1 data, 3 components) and a significantly smaller page.tsx (target: under 400 lines)
</objective>

<execution_context>
@/Users/sunny/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sunny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract techStack data to data/tech-stack.ts</name>
  <files>data/tech-stack.ts</files>
  <action>
Create `data/tech-stack.ts` with:

1. Export a `TechItem` type:

```typescript
export type TechItem = {
  name: string;
  icon: string;
};
```

2. Export the `techStack` array (lines 42-115 of page.tsx):

```typescript
export const techStack: TechItem[] = [
  // ... all 18 tech items from page.tsx
];
```

Copy the exact array content from page.tsx lines 42-115. Do not modify the data.
</action>
<verify>

- File exists: `ls data/tech-stack.ts`
- TypeScript compiles: `npx tsc --noEmit data/tech-stack.ts`
- Has 18 items: `grep -c "name:" data/tech-stack.ts` returns 18
  </verify>
  <done>data/tech-stack.ts exports TechItem type and techStack array with all 18 technologies</done>
  </task>

<task type="auto">
  <name>Task 2: Extract MarqueeText, AnimatedCounter, GitHubActivity to component files</name>
  <files>
    components/marquee-text.tsx
    components/animated-counter.tsx
    components/github-activity.tsx
  </files>
  <action>
**Create `components/marquee-text.tsx`:**
- Add "use client" directive (uses framer-motion)
- Import motion from framer-motion
- Export MarqueeText component (lines 157-179 of page.tsx)
- Export MarqueeTextProps type: `{ text: string; direction?: number }`

**Create `components/animated-counter.tsx`:**

- Add "use client" directive (uses useState, useEffect)
- Import useState, useEffect from react
- Export AnimatedCounter component (lines 182-216 of page.tsx)
- Export AnimatedCounterProps type: `{ value: string; suffix?: string }`

**Create `components/github-activity.tsx`:**

- Add "use client" directive (uses useState, useEffect)
- Import React hooks and lucide-react icons (Github, ExternalLink, GitCommit, Lock, Globe)
- Move GitHubEvent type to this file (lines 118-129 of page.tsx)
- Move formatTimeAgo helper function (lines 131-142)
- Move getCommitMessage helper function (lines 144-154)
- Export GitHubActivity component (lines 219-347)
- Export GitHubActivityProps type: `{ username?: string }`

Each file should be self-contained with all necessary imports. Use named exports for all components and types.
</action>
<verify>

- Files exist: `ls components/marquee-text.tsx components/animated-counter.tsx components/github-activity.tsx`
- TypeScript compiles: `npx tsc --noEmit components/marquee-text.tsx components/animated-counter.tsx components/github-activity.tsx`
  </verify>
  <done>Three component files created with all necessary imports, types, and helper functions extracted from page.tsx</done>
  </task>

<task type="auto">
  <name>Task 3: Update page.tsx to import from new files</name>
  <files>app/page.tsx</files>
  <action>
Modify `app/page.tsx`:

1. **Add new imports** (after existing imports):

```typescript
import { techStack } from "@/data/tech-stack";
import { MarqueeText } from "@/components/marquee-text";
import { AnimatedCounter } from "@/components/animated-counter";
import { GitHubActivity } from "@/components/github-activity";
```

2. **Remove extracted code:**

- Delete techStack array (lines 42-115)
- Delete GitHubEvent type (lines 118-129)
- Delete formatTimeAgo function (lines 131-142)
- Delete getCommitMessage function (lines 144-154)
- Delete MarqueeText component (lines 157-179)
- Delete AnimatedCounter component (lines 182-216)
- Delete GitHubActivity component (lines 219-347)

3. **Verify nothing else changed** - The Page component and all JSX should remain identical, just using imported components instead of inline definitions.

The resulting page.tsx should be approximately 350-380 lines (original 826 minus ~450 lines of extracted code).
</action>
<verify>

- Line count check: `wc -l app/page.tsx` shows under 400 lines
- Build succeeds: `npm run build`
- Dev server starts: `npm run dev` (test manually that page renders)
  </verify>
  <done>page.tsx imports all 4 extracted items and is under 400 lines. Application builds and renders identically.</done>
  </task>

</tasks>

<verification>
1. All new files exist and export expected items
2. `npm run build` succeeds with no errors
3. `npm run dev` - page renders identically to before (visual check)
4. `wc -l app/page.tsx` returns under 400
5. All imports resolve correctly (no TypeScript errors)
</verification>

<success_criteria>

- [ ] data/tech-stack.ts exports techStack array with 18 items and TechItem type
- [ ] components/marquee-text.tsx exports MarqueeText component
- [ ] components/animated-counter.tsx exports AnimatedCounter component
- [ ] components/github-activity.tsx exports GitHubActivity component with types and helpers
- [ ] page.tsx imports from all 4 new files
- [ ] page.tsx is under 400 lines
- [ ] `npm run build` passes
- [ ] Application renders identically (visual parity)
      </success_criteria>

<output>
After completion, create `.planning/phases/07-code-splitting/07-01-SUMMARY.md`
</output>
